#include <stdio.h>

int main()
{

    int age;

    float per;

    printf("\nEnter your age: ");
    scanf("%d", &age);

    printf("\nEnter your per: ");
    scanf("%d", &per);

    printf("\nYour age is: %d", age);

    printf("\nYour per is: %d", per);

    return 0;
}
